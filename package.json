{
  "name": "academic-automation-browser-runtime",
  "private": true,
  "description": "Optional, pinned browser transport for the cnki-download skill",
  "dependencies": {
    "@playwright/cli": "0.1.19"
  }
}
