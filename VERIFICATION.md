# 2.0.0-beta.1 跨平台验证（2026-09-12）

本轮环境：macOS（Darwin）、Python 3.9.6、Node.js 26.3.1、Playwright CLI 0.1.19；依赖树由 package-lock.json 固定。Windows 实机由用户安排测试者稍后验收。

## 本轮证据

- 原有 16 项回归测试保留并通过；缺作者的子进程检查改为跨平台入口。
- 新增跨平台行为测试：JSON 返回值、人工文件恢复、归档中断恢复、批量遇人工步骤停止、浏览器互斥、长脚本文件传递、文件名处理、复制失败保留源文件、包内容与可重复构建等。最终离线运行：34 项通过，1 项隔离浏览器测试按默认设置跳过；该隔离浏览器测试另行启用并通过（19.8 秒）。
- Playwright CLI 的真实隔离 Chrome 测试通过：中文脚本返回值、导航、模拟 CNKI 题名/作者匹配、详情页跳转、PDF 下载保留 Referer、归档、重复运行不重下，以及模拟出版社公开 PDF 获取。
- 上述 Chrome 测试使用隔离配置与本机模拟站点；下载目录由测试显式设定。它不证明官方扩展连接、日常浏览器下载目录或真实学术站点权限可用。
- macOS Apple Events 实机探测发现 Chrome 窗口/标签 id 类型不同，已修复为文本比较。随后得到 Chrome 明确错误：当前关闭 Allow JavaScript from Apple Events。程序返回 needs_user 并提示开启；真实 macOS 学术网站回归未完成。
- 普通离线测试不需要联网或浏览器；可选浏览器测试在 acceptance.md 中单独说明。
- 两个平台包的 ZIP 完整性、内容排除规则、同源重复构建字节一致性、解压后的帮助入口已通过测试。Python/JavaScript/shell 语法检查、文档链接和 git diff --check 通过。
- 系统 skill quick validator 缺 PyYAML，未给产品增加 Python 依赖；使用 Ruby YAML 解析器验证了技能名称、description 与版本元数据。

## 待验收

| 组合 / 场景 | 本轮状态 |
|---|---|
| Windows 官方扩展连接日常 Chrome | 待 Windows 测试者 |
| Windows CNKI、WoS、Scholar、出版社与真实 CAJ | 待 Windows 测试者 |
| Windows 系统保存窗口与真实跨盘/文件占用 | 待 Windows 测试者；已有离线行为测试 |
| macOS Apple Events 真实网站回归 | 需先启用 Chrome JavaScript 权限 |
| macOS 官方扩展与日常下载设置 | 未实测 |
| Codex / Cursor / OpenCode / Qoder 的新版完整工作流 | 待逐一验收；开发过程使用 Codex 不等同于产品验收 |

新包标为 beta，不公开声明未验证组合已正式支持。构建与测试不创建 tag、不上传 Release。以下 1.6.0 记录保留作历史参考，不代表本轮重新实测。

---

# 效率优化与验证记录

验证日期：2026-09-12。真实浏览器使用 macOS Chrome 与已登录的机构会话；原有题名匹配、作者校验、referrer 下载、验证码暂停及 PDF 类型核验保留。

## 已落实的 11 项

| 项目 | 实现 |
|---|---|
| 1. 中文完整流程按需读取 | 移入 `references/chinese-macos.md`，入口只作路由和共用约束 |
| 2. 去重、排错按需加载 | 旧诊断片段与特殊情形移入 `references/troubleshooting-macos.md`；删除入口的重复模式摘要和脚本总表 |
| 3. 缩短 description、明确读取阶段 | description 237 → 130 字符；只做目录不加载出版社下载细节，正常执行不预读所有源码 |
| 4. 页面就绪即继续 | `browser_runtime.py` / `browser_ready.js` 判断目标 URL、元素、加载标记和连续稳定；保留超时与独立访问间隔 |
| 5. 无关下载不阻塞当前论文 | `download_watch.py` 使用本次快照、作者、时间、文件稳定性及类型核验；不再被任意 `.crdownload` 阻塞 |
| 6. 下载全程识别收费/验证码 | 文件等待期间持续检查页面终态，收费或验证出现即停止等待 |
| 7. WoS 按完整性停止滚动 | 边滚动边累计题录，保留被虚拟列表回收的记录；结合槽位、底部和稳定状态判断结束 |
| 8. 检索/题录断点恢复 | `search_resume.py` 逐页/逐 URL 保存 `.progress.json`，重复 URL 去重，相同输入重跑复用；`--refresh` 重取 |
| 9. 未知出版社单标签 | `oa_dl.sh` 通用分支改用共享导航，与已知出版社一致 |
| 10. 批量身份与检索索引 | 批量按归一化题名、作者、目标目录识别条目，并核验路径/大小/修改时间；统一专业检索使用 24 小时索引，缓存命中仍核对实时详情，不符回退 |
| 11. 缺作者提前失败 | 单篇与批量在打开浏览器之前拒绝缺少第一作者的下载条目 |

## 上下文大小

- `SKILL.md`：13,696 → 3,108 字符，减少 **77.3%**。
- description：237 → 130 字符，减少 **45.1%**。
- 这是字符统计，不是精确 token 数。模式详细文档仍在需要时加载；不能将入口缩减比例解释为每项任务全程 token 都减少相同比例。

## 真实网页与文件验证

| 路径 | 结果 |
|---|---|
| CNKI 外文检索 | 查询 `value-added assessment`，页面显示 412 条命中，抽取当前页 20 条 |
| CNKI 外文详情 → 题录 | 两篇详情页成功提取题名、作者、期刊、年份与完整 DOI，生成 2 条带链接目录 |
| CNKI 元数据续跑 | 原命令重跑两篇全部使用已保存记录，无需浏览器导航 |
| CNKI 中文单篇 | 《中高职一体化人才培养增值评价模型研究与应用》，代奕；成功下载归档，3 页 PDF |
| CNKI 批量 | 3 个清单条目对应 2 篇文献；已存在的论文跳过，另一篇下载一次，重复条目跳过；新增论文为 5 页 PDF |
| CNKI 清单重排 | 调换顺序后再次运行，按身份及实际文件全部跳过，未重新下载 |
| CNKI 专业检索索引 | 已建立索引；后续独立测试输出 `INDEX_MATCH`，直接验证详情并下载到隔离目录，没有重做检索，3 页 PDF |
| Google Scholar | 两页各 10 条，共 20 条不重复题录，生成 20 条带链接目录；原命令重跑使用两页缓存 |
| Web of Science | 用户完成一次机构统一认证后，两页分别 50 和 25 条，累计 75 条，与检索计数一致，生成 75 条带链接目录；重跑使用缓存 |
| 未知出版社 → OA PDF | DOI `10.54097/JXAZNN10`，落到 DRPress 的文章页；通用分支找到 OJS PDF 链接，转换为 download 路径并成功归档，7 页 PDF |
| 通用归档包装脚本 | 使用真实 7 页 PDF 在隔离下载目录测试；同时存在无关 `.crdownload`，归档成功且保留该无关文件 |

测试产物位于 `/tmp/cnki-skill-verification/`，没有放入用户现有文献分类目录。该临时目录可能被系统清理。

实测缓存重跑耗时：两篇元数据约 0.043 秒；两页 Scholar 约 0.039 秒。仅为本机单次观察，不是稳定性能基准。首次访问耗时取决于网站与网络；没有声称所有首次检索都更快。WoS 为确认完整的 50/25 条记录，分别用了 19/10 次观察，优先避免漏条。

## 离线回归与静态检查

运行：

```bash
python3 -B -m unittest discover -s tests -v
```

16 项全部通过，覆盖：无关下载、目标未完成文件、多候选拒绝、伪装 PDF、AppleDouble、收费/验证码延迟出现、同名不覆盖、就绪稳定性、前置作者校验、身份重排/文件变化、索引精确匹配/歧义/过期、详情标题、部分结果恢复、元数据 URL 去重、WoS 虚拟卡片回收。

49 个附属脚本（11 Python、9 shell、29 JavaScript）的语法检查通过；本地参考链接、YAML frontmatter 与 `git diff --check` 通过。系统的 skill quick validator 缺少 PyYAML，因此 YAML 由 macOS Ruby 的 YAML 解析器检查，未给技能增加第三方运行依赖。

## 实测发现并修正的兼容点

- CNKI 页面含隐藏的验证码模板：只凭正文包含提示会误停；改为结合可见位置识别，保留真实验证码停止行为。
- CNKI 中文篇名结果页不总有 `#txt_search`：切库前就绪允许使用真实语言切换控件。
- CNKI 第一个 `h1` 可能是隐藏的“自动登录”标题：索引详情核验使用真实文章 `document.title` 和作者区域，防止每次命中都无效回退。

真实验证码/收费发生在下载过程中的分支通过离线模拟验证，未刻意在真实网站触发；CAJ、所有出版社、所有机构代理及长批次没有逐一实测。现有样本证明主要链路可运行，不代表第三方站点所有页面都永久兼容。
