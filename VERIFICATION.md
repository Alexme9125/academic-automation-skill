# Beta 4 发布验证（2026-09-13）

版本 `2.0.0-beta.4`，分支 `codex/cross-platform-browser`。本次将 PubMed 开发构建及后续连接、文件核验和使用规则修复纳入同一个公开 Pre-Release，保留 Beta 3 的标签和附件。

- 发布前离线回归共 145 项：144 项通过，1 项隔离 Chrome 测试按配置跳过；禁用可选第三方 Python 包后，同一套 145 项检查结果一致。本次没有重新运行真实网站或机构会话。
- 两端 ZIP 核对 SHA-256、内容排除项和普通字号加粗的权限提示；在中文和空格路径解压，检查版本、默认后端及 PubMed 接口环境入口。macOS 使用包内 `academic.command`；Windows 包在 macOS 检查共享 Python 入口及 `academic.cmd` 内容，原生 Windows 执行仍待测试者验收。
- Skill 元数据验证及差异格式检查通过。发布说明在最前面强调无障碍权限的范围和“有安全顾虑请勿使用”，两个包均包含同样的中英文 README 提示。
- 下文保留不同阶段的实测证据；Oxford 后续 Apple Events 自动保存结果见[原生保存验收](references/macos-pdf-save-acceptance.md)。Windows、其他 Harness、中文保存窗口和批量保留标签页仍待分别验收。

发布功能与限制见[Beta 4 说明](RELEASE_NOTES.md)。本次验证输出保存在忽略目录 `tmp/beta4-release/` 及 `tmp/beta4-*-tests.log`，不随安装包分发。

## 当前使用规则：macOS 仅 Apple Events

按用户最新要求，macOS Skill 流程仅使用 Apple Events / System Events，不再使用或回退到 Playwright；下方 macOS 扩展实验只作历史证据，不构成继续使用的指引。Playwright 查看器自动保存尚未通过稳定性验收，Windows 自动保存受阻且无落盘时，须先询问“仅题录”或“保留标签页、批量手动下载”，按实际选择调整交付。

两种语言 README 和两端安装说明均以普通字号加粗突出系统无障碍权限及“有安全顾虑请勿使用”。本次修改为 Skill、文档及验收规则，未修改运行源码；核对文档链接、Skill 元数据、双平台打包和解压入口。保留标签页的批量人工编排新增于本次指引，仍需真实 Harness 验收，不能以已有离线测试或此前三个自动归档样本证明该编排已通过。

## 2026-09-13：扩展 Token 前置流程

按用户要求，新建 Playwright 扩展连接前必须先提供进程环境 `PLAYWRIGHT_MCP_EXTENSION_TOKEN`。缺少或仅含空白时，连接入口在启动 Playwright 和修改已有连接元数据前返回 `needs_user`；已有会话沿用。`doctor` 只报告 Token 是否存在，不输出值、不宣称认证已验证。同步更新 Skill 及两端安装说明。

145 项离线用例：144 项通过，1 项隔离 Chrome 测试按配置跳过。新增验证缺少 Token 时零连接调用且保留已有元数据、提供后连接而不泄漏值、已有会话与纯接口检查不重复要求 Token。本次未新建真实浏览器连接，保留 Oxford 原生保存的待回复状态；平台实测结论沿用下文证据。

## 2026-09-13：Playwright 扩展完整性与恢复修复

完整实现、证据和平台边界见[修复验收记录](references/playwright-repair-acceptance-20260913.md)。本轮知网、DRPress 和 Frontiers 各一篇已取得完整文件；CLI 身份核验 2 篇，Frontiers 用已确认题录另作验收补核。Oxford 已通过文章页验证并打开正文 PDF，标签恢复修正后待回复继续原生保存，尚不能宣称扩展查看器保存通过。Windows 继续由测试者验收。


以下为历史 PubMed 开发阶段验证：当时版本 `2.0.0-beta.3.pubmed.1`，在 `codex/cross-platform-browser` 实现并准备本地测试包，尚未公开发布；相关功能现随 Beta 4 分发。测试在 macOS / Codex 本地命令环境完成，不替代 Windows 或其他 Harness 的验收。

## 真实样本结果

检索表达式为 `37935836[uid] OR 31647093[uid] OR 28527048[uid]`，选择 `free-only`。官方接口返回总数 3、抽取 3，保存实际查询转换。接口获取完整作者、日期、摘要、DOI、PMCID及 LinkOut 全文来源。PMID 28527048 的勘误 PMID 28664250 单独记录，没有替换原文 DOI。

| PMID | 获取路径 | 文件大小 | 页数 | 正文自动核验 |
|---|---|---:|---:|---|
| 37935836 | PMC10719501.1，正式发表版本，官方 HTTPS 自动获取 | 549078 B | 12 | 题名及第一作者通过 |
| 28527048 | PMC5506233.1，正式发表版本，官方 HTTPS 自动获取 | 767951 B | 13 | 题名及第一作者通过 |
| 31647093 | Oxford Academic，macOS Apple Events；用户完成验证并手动保存后自动核验归档 | 638027 B | 10 | 题名及第一作者通过 |

共归档 3 篇，正文自动核验 3 篇，合计 35 页。页数只作辅助；身份结论来自首页题名和作者检查。SHA-256：

- PMID 37935836：`cb0c475464593b8e2ec2e5646424961eb6876a0c4b15b40acb3fdd8187bf9d4a`
- PMID 28527048：`c5c5c6de4cb8e066503d6a94bcecbc78c9c5bb5067189de6ba09d00f2936e1b9`
- PMID 31647093：`ae68e1c15806eb5f8e94ba56ae9151d8947b534a0522f98af62508f5311ad7b3`

Oxford 实测先遇原绑定标签关闭，收到用户“已就绪”后才重新连接；随后自然出现人机验证，收到“已完成验证”后才继续；自动下载未落盘时等待保存，收到“已保存”后直接核验文件并归档，没有再次下载。三篇清单重排后复跑全部使用已有文件，未产生重复文件。断开连接后 Chrome 窗口保留。

本机 pypdf 6.15.0 首次核验通过；独立虚拟环境安装要求文件锁定的 6.18.1 后，三篇复核也通过。用禁用 site-packages 的 Python 运行独立真实 PDF 副本的人工归档，成功返回 `unverified / pypdf_not_installed` 和用途、安装、潜在遗漏说明。关闭 Node 路径、缺少 pypdf、选用未连接的 extension 后端时，接口环境检查和三篇真实 PubMed 检索仍完成，未要求打开 Chrome。

## 自动验证及边界

- 离线套件 103 项：102 项通过、1 项隔离 Chrome 测试默认跳过，原 67 项保留。新增覆盖四来源范围缺失、实际回复补回、三种范围分支、布尔查询、排序/免费筛选、分页与部分记录恢复、缺字段、书籍记录、勘误、PMC 多版本/作者稿/无 PDF/错误身份、限流、HTML 伪装、补充材料、可选 pypdf、明确错篇、复制损坏和不重复下载。
- 隔离 Chrome 传输测试单独通过，29.3 秒：真实可见点击、CNKI 作者与 Referer、出版社事件文件捕获、重复运行不再请求。出版社无需额外保存监听器。只使用本机网页与临时配置，不等于官方扩展已附着日常 Chrome。
- macOS 可选官方扩展连接已尝试，Playwright 明确返回未安装扩展；本项尚未通过，不以隔离 Chrome 测试替代。
- Windows 原生扩展、中文/空格/长路径、跨盘、文件占用和系统保存仍由测试者验收。其他模型/Harness 的两步范围询问和人工暂停行为待复测；本轮只实测 Codex 的实际验证与保存交接，四来源范围停止由 CLI 回归覆盖。
- 未重跑全部 CNKI、Scholar、WoS 真实站点流程；相关旧逻辑与脚本离线回归通过。范围分类为未知的条目单列，不能声明是收费。
- 本地测试包使用独立开发版本号，包含可选 `requirements-pdf.txt`，不捆绑 Python、Node、Chrome、浏览器状态、文献或本机配置。两包在 macOS 中文与空格路径解压，版本、默认后端、帮助、PubMed 题录生成和接口环境检查通过；Windows 包的 Python 入口已检查，原生 academic.cmd 仍待 Windows。46 个 Python、33 个 JavaScript、9 个 shell 文件语法，以及 Skill frontmatter、本地文档链接、校验和和私密/临时产物排除检查通过。

本地详细输出保存在项目忽略目录 `tmp/pubmed-acceptance-20260913/`；文献、窗口标识和会话信息不进入源码或分发包。后续测试步骤见 [PubMed 验收](references/acceptance.md#pubmed-开发构建验收)。

---

# 2.0.0-beta.3 验证：Windows DSH 反馈（2026-09-13）

依据《DSH测试全流程回报_cnki-download》修复。报告中的测试环境为 Windows、DSH 与 DeepSeek；本轮开发和验证仍在 macOS，未取得测试者的原始运行脚本或失败文章的下载事件记录。因而可以确认下面的程序缺陷与修复行为，但不能据此断言已解决该 Windows 环境的全部故障。

| 问题 | 修复及验证 |
|---|---|
| 自写脚本直接导入工作流，遗漏锁和待人工记录 | 公开下载/知网检索函数要求统一入口的任务上下文；直接调用在浏览器操作前返回 64。新增 `search cnki`，支持主题与专业检索，逐页保存进度 |
| 用户确认后，页面超时导致原暂停记录消失 | 就绪超时返回 70 并保留已有 pending；其他任务仍被拦截；批量在 70/75 时停止 |
| 扩展连接报告成功，但实际会话不可用 | 附着后读取真实标签标题与 URL，通过才保存连接；失效连接会清除本地连接标记并提示重连 |
| 页面跳转与读取竞态 | 导航等待文档提交，超时只接受已替换旧文档的页面；只读探测最多重试三次上下文切换，提交和点击不重放；匹配与导航分开执行 |
| 自动下载失败、人工点击后才有文件 | 扩展后端选择唯一可见的 PDF/CAJ 控件并实际点击，监听所选页及其新窗口的下载事件；保存事件对应文件后核验作者和格式。无事件时检查原下载目录，无可核验文件则暂停，不追加点击 |

下载检查点新增控件、下载事件和保存错误信息，便于复测定位。事件文件先写入短名 `received.part`，确认保存完成后才参与归档；最终恢复原文件名。未确认的残留传输不触发新请求。详情身份、来源 Referer、文件核验及同名不覆盖机制保留。

## 实际验证

- 离线套件共 67 项：66 项通过，1 项隔离 Chrome 测试默认跳过。新增 15 项回归涵盖上述入口、暂停、连接、竞态、事件保存、无事件恢复及只点击一次的行为。
- 单独启用隔离 Chrome 测试通过（28.8 秒）。本机模拟详情页的可见按钮只有点击事件能发起下载，并要求可信点击；隐藏的误导链接不会被选中。统一 CLI 成功归档 PDF，检查点记录下载事件，保留详情页 Referer，重复运行没有第二次下载。
- 同一隔离 Chrome 测试还回归了出版社直接下载及独立 HTTP 失败后的浏览器下载。它使用临时浏览器配置和本机页面，不访问机构账户，也不验证官方扩展的真实附着。
- Python、JavaScript 和 shell 语法检查通过；打包、内容排除、重复构建、解压帮助入口及文档链接回归通过。
- Beta 3 发布包分别在 macOS 的中文和空格路径下解压，核对版本、默认后端与校验和，启动帮助和题录生成通过；macOS 使用包内 `academic.command`，Windows 包通过 Python 入口检查，不能代替 Windows 原生 `academic.cmd` 验收。

Windows 测试者仍需复测真实扩展连接、原来需要手点的文章、中文与空格路径、系统保存窗口及人工恢复，见[Windows DSH 复测清单](references/acceptance.md#windows-dsh-反馈修复复测)。本轮也未重新验收真实 macOS Apple Events 网站流程。

本节修复随 Beta 3 的两个平台包分发；此前版本号仍为 Beta 2 的开发分支快照也可能包含这些改动，所以测试者应同时记录版本和 `code_fingerprint`。公开 Beta 2 的标签及附件保留。入口保护不构成本地代码安全沙箱；Harness 仍须保证 `resolve` 依据实际用户回复。

---

# 2.0.0-beta.2 验证：外部模型 / Harness 反馈（2026-09-12）

依据用户提供的《脑机接口文献下载测试记录》定位问题。记录涉及 macOS、ZCode 与 GLM-5.3，但未记录 Skill 的版本或代码指纹，原始下载及运行文件已清理。因此，这里区分报告中的现象、当前源码可确认的问题和本轮实际验证，不将测试记录中的经验建议当作操作指令。

## 判断与修复

| 反馈 | 当前修复 |
|---|---|
| Wiley / Elsevier 验证未完成便结束下载 | `needs_user` 单独持久化；必须提问并等待实际用户回复。未回复时，其他文章、其他会话名及旧导航/JS 入口均被拦截 |
| 默认把失败归为无机构订阅 | 删除题录模板“失败则标机构订阅”和外文文档中的默认推断。期刊为订阅制、无机构标识或独立 HTTP 失败均不足以判定该篇无权限 |
| 内嵌 PDF 尚未保存便跳到下一篇 | 暂停当前任务，优先从检查点恢复已落盘文件。用户决定恢复时只放行原任务；明确跳过则持久保留 `skipped_by_user`，批量不会再次请求 |
| 旧包装器把待人工映射到退出码 3 | `oa_dl.sh` 保留统一退出码 2；元数据与其他旧入口接入同一调度约束。新增退出码 6 表示用户明确跳过 |
| JMIR 误选 `app3.pdf` | 正文元数据链接与明确的正文 PDF 优先，剔除附录/补充材料候选，保留实际下载来源；页数不能单独证明正文身份 |
| Cloudflare / AWS WAF 识别不足 | 补充可见安全验证页面识别。文章页通过检查后，仅对同源正文链接尝试下载属性；该属性不保证跨源或所有保存设置下都生效 |
| 知网详情链接失效 | 识别 404 并返回失效 URL、已完成数量和进度位置；保留已完成记录。先刷新同一篇来源链接，不能静默替换用户指定篇目 |
| DOI 尾部带追踪参数 | 比较候选长度前去除已识别的追踪参数，保留括号及 DOI 自身字符；输出题录和 DOI 下载入口也规范化 |
| 重复文件名归档未命中 | 原记录没有完整参数和候选快照，不能确认具体根因。本轮补充作者后无空格的 `(1)` 后缀支持；多候选仍拒绝猜测，使用显式文件归档 |
| 不确定不同 Harness 实际装了哪份代码 | `doctor` 返回 `code_fingerprint`，复测记录需同时保留版本、指纹和模型/Harness 信息 |

`download` 属性的边界按 [MDN 说明](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#download)处理；未采纳报告中“可靠通用手段”的概括，也没有实现验证码绕过。

## 本轮验证

- 新增 15 项行为回归，覆盖跨进程暂停、禁止换任务、实际回复后的原任务恢复、再次验证重新暂停、用户跳过后批量续跑、已存文件先恢复、正文/附录选择、DOI 清洗、404 进度保留和重复文件名。
- 离线套件共 52 项：51 项通过，1 项隔离 Chrome 测试默认跳过，原有回归全部通过。
- 单独启用隔离 Chrome 测试通过（25.7 秒）。包含模拟站点的中文匹配下载，以及独立 HTTP 请求不能获取 PDF 后，从真实 Chrome 的同源链接下载并保留 Referer。
- 38 个 Python 文件、31 个 JavaScript 文件和 9 个 shell 文件通过语法检查。系统 Skill 验证器因缺少 PyYAML 无法执行；另用 Ruby YAML 验证 Skill 元数据。
- Beta 2 两个平台 ZIP 已在 macOS 的中文和空格路径下解压，版本、默认后端、帮助入口、题录生成及安装说明链接检查通过，SHA-256 校验一致。macOS 使用包内 `academic.command`；Windows 包在 macOS 上验证 Python 入口，不能代替 Windows 原生 `academic.cmd` 验收。

本轮没有重新运行 ZCode/GLM-5.3，也没有把模拟站点结果当成 Wiley、Elsevier 或真实 AWS WAF 的重新验收。实际模型是否遵守等待规则仍需按 [Harness 复测清单](references/acceptance.md#模型与-harness-的人工交接复测)验证。

程序无法鉴别模型是否虚构了用户回复，也不能禁止拥有本地命令权限的 Agent 绕过本项目入口。Harness 应将 `wait_for_user=true` 设为暂停调度信号，并仅在收到实际用户消息后开放 `browser resolve`。这一层与 Skill 和 CLI 的约束共同使用。

本节修复随 beta.2 的 Windows 与 macOS 安装包分发。以下为 beta.1 的历史验证记录，不表示本轮重新完成了所有真实网站验收。

---

# 2.0.0-beta.1 跨平台验证（2026-09-12）

本轮环境：macOS 27.0、Chrome 152.0.7977.83、Python 3.9.6、Node.js 26.3.1、Playwright CLI 0.1.19。Windows 实机由测试者稍后验收；下文明确区分真实网站、隔离模拟站点与离线行为检查。

## macOS 真实网站与安装包

使用用户已登录的日常 Chrome，默认 Apple Events 后端。启用 Allow JavaScript from Apple Events 后，浏览器连接和脚本执行检查通过；上述测试均未使用 Playwright 官方扩展。

| 项目 | 结果 |
|---|---|
| CNKI 中文完整流程 | 《中高职一体化人才培养增值评价模型研究与应用》，第一作者代奕；题名/作者匹配、详情跳转、下载与归档成功，实际文件为 3 页 PDF |
| 中文重复运行 | 返回 `cached: true`，校验已有路径和文件后跳过，没有再次下载 |
| CNKI 外文 | `value-added assessment` 显示 412 条命中，当前页提取 20 条；从 2 篇详情读取题名、作者、期刊与完整 DOI，生成 2 条带链接目录 |
| 外文题录续跑 | 两篇详情重跑均输出 `META_CACHED`，无需重新导航 |
| Google Scholar | 两页分别 10 条，最终 20 条去重题录；目录生成成功，重跑两页均使用缓存 |
| 出版社公开 PDF | DOI `10.54097/JXAZNN10` 经 DRPress 文章页和 OJS download 链接下载归档，核验为 7 页 PDF |
| WoS 登录与分页 | 首次到机构认证页返回 `needs_user` / 退出码 2；用户登录后从解压包运行，两页分别 50 和 25 条，累计 75 条；目录生成成功，重跑使用两页缓存 |
| 批量与旧入口 | 3 行清单包含 2 个已下载目标和 1 行重复项，重排前后均按文件核验跳过；包内旧 `cnki_dl.sh` 返回成功 |
| 断开连接 | 解压包执行 `browser disconnect` 后，Chrome 窗口及标签 id 集合与执行前一致 |
| 解压包默认环境 | 从含中文和空格路径运行 `academic.command`；不安装 npm 依赖且 PATH 中不提供 Node.js，`doctor --browser` 返回成功，Node 与 Playwright CLI 均报告未安装 |

测试文件和日志在 `/tmp/academic-prerelease-verification/`，安装包测试在 `/tmp/academic-prerelease-package-check/`。只将本次匹配的下载归档到临时目录，没有将文献或浏览器连接状态放入仓库或发布附件。临时目录可能被系统清理。

## 本轮修复

- Chrome 接受页面跳转后，Apple Events 曾在旧文档仍可读取时返回，导致出版社流程误报没有 PDF 链接。现在等待新文档替换旧文档，并兼容机构代理跳转；真实出版社下载及旧文档等待测试通过。
- WoS 机构认证页此前表现为等待超时，现在识别可见的密码登录表单并提示人工处理；隐藏的登录表单不会触发该判断。
- 修复安装说明从 `references/` 复制到 ZIP 根目录后相对链接失效的问题；测试逐一检查解压后的本地链接。
- 上轮已修复 Chrome 窗口/标签 id 的类型差异，本轮真实连接与网站操作通过。

## 离线与隔离验证

- 共 37 项测试：36 项通过，1 项隔离 Chrome 测试默认跳过；原有 16 项全部保留并通过。
- 单独启用隔离 Chrome 测试通过（20.1 秒）：中文脚本返回值、模拟 CNKI 题名/作者匹配、详情跳转、下载请求 Referer、PDF 归档及重复运行，以及模拟出版社公开 PDF。
- 隔离 Chrome 使用临时配置和本机模拟站点，下载目录由测试指定。该结果不代表 Windows 官方扩展、日常下载设置或机构权限已验收。
- 离线检查覆盖人工文件恢复、归档中断恢复、批量遇人工步骤停止、浏览器互斥、复制失败保留源文件、Windows 文件名处理及多候选拒绝等；没有用离线结果代替真实 Windows 测试。
- 两个平台 ZIP 的完整性、内容排除规则、同源重复构建字节一致性、解压后帮助入口和安装说明链接检查通过。
- 35 个 Python 文件、31 个 JavaScript 文件与 9 个 shell 文件的语法检查通过。

## 尚待验收

| 组合 / 场景 | 本轮状态 |
|---|---|
| Windows 官方扩展连接日常 Chrome | 待 Windows 测试者 |
| Windows CNKI、WoS、Scholar 与出版社流程 | 待 Windows 测试者 |
| Windows 真实跨盘归档、文件占用与系统保存窗口 | 待 Windows 测试者；已有离线行为测试 |
| macOS 官方扩展与扩展下载设置 | 未实测 |
| 真实 CAJ、验证码完成后的自动续下、特殊 PDF 保存窗口 | 本轮未实测；人工文件恢复有离线测试 |
| Codex | 本轮由 Codex 调用统一入口完成上述 macOS 检索与下载；不代表其他平台已通过 |
| Cursor / OpenCode / Qoder | 新版完整调用流程待逐一验收 |

本版作为 Pre-Release 分发。以下 1.6.0 记录仅供历史参考，不能用于填补本轮未测项。

---

# 效率优化与验证记录

2026-09-13 新增：[macOS PDF 查看器自动保存验收](references/macos-pdf-save-acceptance.md)。Oxford 线上 PDF 已通过原生保存、正文核验及续跑去重；118 项回归中 117 项通过，1 项可选浏览器测试跳过。下文保留此前验证记录及其当时的测试数量。

验证日期：2026-09-12。真实浏览器使用 macOS Chrome 与已登录的机构会话；原有题名匹配、作者校验、referrer 下载、验证码暂停及 PDF 类型核验保留。

## 已落实的 11 项

| 项目 | 实现 |
|---|---|
| 1. 中文完整流程按需读取 | 移入 `references/chinese-macos.md`，入口只作路由和共用约束 |
| 2. 去重、排错按需加载 | 旧诊断片段与特殊情形移入 `references/troubleshooting-macos.md`；删除入口的重复模式摘要和脚本总表 |
| 3. 缩短 description、明确读取阶段 | description 237 → 130 字符；只做目录不加载出版社下载细节，正常执行不预读所有源码 |
| 4. 页面就绪即继续 | `browser_runtime.py` / `browser_ready.js` 判断目标 URL、元素、加载标记和连续稳定；保留超时与独立访问间隔 |
| 5. 无关下载不阻塞当前论文 | `download_watch.py` 使用本次快照、作者、时间、文件稳定性及类型核验；不再被任意 `.crdownload` 阻塞 |
| 6. 下载全程识别收费/验证码 | 文件等待期间持续检查页面终态，收费或验证出现即停止等待 |
| 7. WoS 按完整性停止滚动 | 边滚动边累计题录，保留被虚拟列表回收的记录；结合槽位、底部和稳定状态判断结束 |
| 8. 检索/题录断点恢复 | `search_resume.py` 逐页/逐 URL 保存 `.progress.json`，重复 URL 去重，相同输入重跑复用；`--refresh` 重取 |
| 9. 未知出版社单标签 | `oa_dl.sh` 通用分支改用共享导航，与已知出版社一致 |
| 10. 批量身份与检索索引 | 批量按归一化题名、作者、目标目录识别条目，并核验路径/大小/修改时间；统一专业检索使用 24 小时索引，缓存命中仍核对实时详情，不符回退 |
| 11. 缺作者提前失败 | 单篇与批量在打开浏览器之前拒绝缺少第一作者的下载条目 |

## 上下文大小

- `SKILL.md`：13,696 → 3,108 字符，减少 **77.3%**。
- description：237 → 130 字符，减少 **45.1%**。
- 这是字符统计，不是精确 token 数。模式详细文档仍在需要时加载；不能将入口缩减比例解释为每项任务全程 token 都减少相同比例。

## 真实网页与文件验证

| 路径 | 结果 |
|---|---|
| CNKI 外文检索 | 查询 `value-added assessment`，页面显示 412 条命中，抽取当前页 20 条 |
| CNKI 外文详情 → 题录 | 两篇详情页成功提取题名、作者、期刊、年份与完整 DOI，生成 2 条带链接目录 |
| CNKI 元数据续跑 | 原命令重跑两篇全部使用已保存记录，无需浏览器导航 |
| CNKI 中文单篇 | 《中高职一体化人才培养增值评价模型研究与应用》，代奕；成功下载归档，3 页 PDF |
| CNKI 批量 | 3 个清单条目对应 2 篇文献；已存在的论文跳过，另一篇下载一次，重复条目跳过；新增论文为 5 页 PDF |
| CNKI 清单重排 | 调换顺序后再次运行，按身份及实际文件全部跳过，未重新下载 |
| CNKI 专业检索索引 | 已建立索引；后续独立测试输出 `INDEX_MATCH`，直接验证详情并下载到隔离目录，没有重做检索，3 页 PDF |
| Google Scholar | 两页各 10 条，共 20 条不重复题录，生成 20 条带链接目录；原命令重跑使用两页缓存 |
| Web of Science | 用户完成一次机构统一认证后，两页分别 50 和 25 条，累计 75 条，与检索计数一致，生成 75 条带链接目录；重跑使用缓存 |
| 未知出版社 → OA PDF | DOI `10.54097/JXAZNN10`，落到 DRPress 的文章页；通用分支找到 OJS PDF 链接，转换为 download 路径并成功归档，7 页 PDF |
| 通用归档包装脚本 | 使用真实 7 页 PDF 在隔离下载目录测试；同时存在无关 `.crdownload`，归档成功且保留该无关文件 |

测试产物位于 `/tmp/cnki-skill-verification/`，没有放入用户现有文献分类目录。该临时目录可能被系统清理。

实测缓存重跑耗时：两篇元数据约 0.043 秒；两页 Scholar 约 0.039 秒。仅为本机单次观察，不是稳定性能基准。首次访问耗时取决于网站与网络；没有声称所有首次检索都更快。WoS 为确认完整的 50/25 条记录，分别用了 19/10 次观察，优先避免漏条。

## 离线回归与静态检查

运行：

```bash
python3 -B -m unittest discover -s tests -v
```

16 项全部通过，覆盖：无关下载、目标未完成文件、多候选拒绝、伪装 PDF、AppleDouble、收费/验证码延迟出现、同名不覆盖、就绪稳定性、前置作者校验、身份重排/文件变化、索引精确匹配/歧义/过期、详情标题、部分结果恢复、元数据 URL 去重、WoS 虚拟卡片回收。

49 个附属脚本（11 Python、9 shell、29 JavaScript）的语法检查通过；本地参考链接、YAML frontmatter 与 `git diff --check` 通过。系统的 skill quick validator 缺少 PyYAML，因此 YAML 由 macOS Ruby 的 YAML 解析器检查，未给技能增加第三方运行依赖。

## 实测发现并修正的兼容点

- CNKI 页面含隐藏的验证码模板：只凭正文包含提示会误停；改为结合可见位置识别，保留真实验证码停止行为。
- CNKI 中文篇名结果页不总有 `#txt_search`：切库前就绪允许使用真实语言切换控件。
- CNKI 第一个 `h1` 可能是隐藏的“自动登录”标题：索引详情核验使用真实文章 `document.title` 和作者区域，防止每次命中都无效回退。

真实验证码/收费发生在下载过程中的分支通过离线模拟验证，未刻意在真实网站触发；CAJ、所有出版社、所有机构代理及长批次没有逐一实测。现有样本证明主要链路可运行，不代表第三方站点所有页面都永久兼容。
